# Local dev: leave this unset — Vite proxies /api to the local Worker on :8787.
#
# Production (Cloudflare Pages build): set this to your deployed Worker URL + /api, e.g.
# VITE_API_BASE="https://friday-api.your-subdomain.workers.dev/api"
